# Lab 2 — Peer Review Record 

**Author:** กมนนัทธ์ สีทาไข — 67070501001 — GitHub: @Kamonnatt23

**Peer reviewer:** เบญญาภา รัตนคุโณดม — 67070501030 — GitHub: @Piink7878

## Pull Requests I authored (reviewed by my partner)
| PR | Reviewer | Decision | Comment | My Response | Evidence |
|----|----------|----------|---------|-------------|----------|
| PR #21 docs: add lab 2 specification, test plan, ui and api specs | Piink7878 | requested change | โดยรวมโอเคเลย แต่มีจุดที่อยากให้แก้ก่อน merge นิดหน่อยนะ POST /api/attachments กับ /api/attachments/upload ในแต่ละ spec ไม่ตรงกัน รบกวนเลือกให้เหลืออันเดียว<br>Related System ระบุว่า required ในบางที่ แต่ใน BR-02 ไม่ได้ใส่ไว้ รบกวนทำให้ตรงกัน<br>GET /api/attachments/:id/download มีใน api-spec.md แต่ไม่มีใน specification.md น่าจะเพิ่มให้ครบ<br>Test ยังไม่ค่อยครอบคลุมพวก pagination/filter/sort และกรณี user พยายามเข้าถึง ticket ของคนอื่น โดยเฉพาะเรื่อง user isolation น่าจะเพิ่ม test ไว้หน่อย<br>Flow ของ attachment ที่ upload ก่อนสร้าง ticket ยังไม่ค่อยชัด โดยเฉพาะกรณีกด Remove ก่อน submit อยากให้ระบุ behavior ไว้นิดนึง<br>นอกนั้นโอเคเลยย เย่ ๆ | Updated upload endpoint to POST /api/attachments, added Related System to BR-02, clarified pre-submission attachment removal flow, and added tests T-11 to T-13 for security and pagination | https://github.com/Kamonnatt23/toktickit/pull/21 |
| PR #22 feat: implement development requester context | Piink7878 | requested change | เหลืออีกนิดเดียววว ใน DevContext ตอนนี้ยัง restore user จาก localStorage ก่อน API จะโหลดเสร็จ ถ้า API fail คนที่เคยเลือกไว้จะยังถูกมองว่า active อยู่ อยากให้รอ validate กับ active users จาก API ก่อนค่อย restore context<br>test stale user ตอนนี้เช็กแค่ว่าสุดท้าย localStorage ถูกลบ อยากให้เพิ่ม assert ว่า user ที่ stale ไม่ถูก set เป็น activeUser ด้วย<br>นอกนั้นโอเคและ | แก้ไข<br><br>DevContext Race Condition (client/src/contexts/DevContext.tsx)<br>• ยกเลิกการเรียก setActiveUser(initialUser) ทันทีตอนเริ่มต้น (Synchronous) เพราะอาจทำให้ UI<br>กระพริบเป็นหน้าผู้ใช้แล้วเด้งออกหากตรวจสอบพบว่าไม่ Valid<br>• เปลี่ยนเป็นอ่านค่าจาก localStorage เก็บไว้ในตัวแปร และรอให้ API GET /api/dev/users โหลดเสร็จก่อน<br>• หาก API คืนค่ากลับมาสำเร็จ และ ตรวจพบว่า User เดิมยังมีตัวตนอยู่ ถึงจะเรียก setActiveUser เพื่อตั้งสถานะเข้าสู่ระบบ แต่ถ้า API<br>ล้มเหลวหรือไม่มีชื่อ User ในระบบแล้ว สถานะ activeUser จะยังคงเป็น null และถูกเคลียร์ออกจาก localStorage อย่างแนบเนียน<br>Stale User Test Assertion (client/tests/lab-02/DevContext.test.tsx)<br>• อัปเดตเคส clears stale/invalid user in localStorage... โดยเพิ่มคำสั่ง expect(screen.queryByText(/[TESTING MODE]/i)).<br>not.toBeInTheDocument() ทั้งก่อนและหลัง API โหลดเสร็จ เพื่อยืนยันว่า activeUser ไม่ถูกเซ็ตเลยจริงๆ<br>และแบนเนอร์ล็อกอินจะไม่ปรากฏขึ้นมากวนใจ<br>• (แถม) อัปเดตเคส allows changing requester ให้รองรับ Asynchronous Rendering ที่เปลี่ยนไปจากการแก้ไขข้อ 1<br>เพื่อให้เทสต์กลับมาผ่านได้อย่างสมบูรณ์<br>ช่วยมาตรวจอีกรอบให้หน่อยนะคับ | https://github.com/Kamonnatt23/toktickit/pull/22 |
| PR #23 Feature/3 ticket creation | Piink7878 | requested change | มีจุดที่อยากให้แก้เพิ่มให้ครบ Issue 3 ตาม lab spec:<br><br>ตอนนี้ Create Ticket ยังไม่มี Attachment ทั้งที่ต้องรองรับ type/size/max files ตาม spec<br>หลังสร้างสำเร็จควรเอา official Ticket Number จาก backend มาแสดง ไม่ใช่แค่ “Ticket created successfully!”<br>Validation ตอนนี้เช็กแค่ required อยากให้เพิ่ม length / trim / allowed values และ backend validation ด้วย<br>ถ้าโหลด Category / Related System ไม่สำเร็จ ตอนนี้ยังไม่มี error state ให้ user เห็น<br>ยังไม่เห็น tests ของ API/UI สำหรับ create ticket อยากให้เพิ่ม happy path + invalid/boundary + API failure cases ด้วย<br>โดยรวม flow หลักโอเคแล้ว เหลือพวก requirement กับ test coverage ให้ครบ | ขอบคุณสำหรับรีวิว สำหรับข้อ 1 เรื่องระบบ Attachment เราได้แพลนแยกไปทำใน Issue 6: Attachment management เพื่อให้โค้ดเป็นสัดส่วนและโฟกัสทีละเรื่อง ใน Issue 3 นี้จะยังไม่มีส่วนของไฟล์ ส่วนข้อ 2-5 เห็นด้วยเลย ก็เลยได้แก้ขเรียบร้อยแล้วค่ะ<br>สรุปสิ่งที่มีการแก้ไขไปดังนี้:<br><br>เพิ่มหมายเลขตั๋วอย่างเป็นทางการ (Official Ticket Number):<br>• Backend (app.ts): ปรับ API POST /api/tickets ให้มีการสร้างหมายเลขตั๋วอัตโนมัติจาก ID ในฐานข้อมูล (เช่น TKT-001, TKT-002) และส่งกลับมาเป็น Response<br>• Frontend (CreateTicket.tsx): อัปเดตข้อความแจ้งเตือนสำเร็จ ให้แสดงหมายเลขตั๋วอย่างชัดเจน เช่น "Ticket TKT-001 created successfully!"<br>เพิ่มความเข้มงวดในการตรวจสอบข้อมูล (Strict Validation):<br>• ใส่คำสั่ง .trim() ทั้งหน้าบ้านและหลังบ้าน เพื่อป้องกันการพิมพ์แค่ช่องว่างเปล่าๆ มา<br>• จำกัดความยาวของ Summary ไม่เกิน 100 ตัวอักษร และ Description ไม่เกิน 1000 ตัวอักษร<br>• ล็อกค่า Priority ให้ส่งมาได้แค่ Low, Medium, High, หรือ Critical เท่านั้น (หากส่งค่าแปลกๆ มา API จะปฏิเสธทันที)<br>รองรับกรณี Dropdown โหลดข้อมูลพัง (API Error States):<br>• หาก API ของหมวดหมู่ (Category) หรือระบบที่เกี่ยวข้อง (Related System) โหลดไม่สำเร็จ (เช่น เน็ตหลุด หรือ API พัง) หน้าเว็บจะไม่นิ่งเงียบ แต่จะแสดงกล่องแจ้งเตือน Error สีแดงให้ผู้ใช้ทราบทันทีที่ด้านบนของฟอร์ม<br>เพิ่มความครอบคลุมของการทดสอบ (Test Coverage):<br>• ฝั่ง Client (CreateTicket.test.tsx): เพิ่มเคสจำลองการกรอกตัวอักษรเกินกำหนด (Boundary cases), เคส API Dropdown โหลดพัง,และเคสทดสอบการแสดงผล Ticket Number อย่างละเอียด<br>• ฝั่ง Server (tickets.api.test.ts): เพิ่มเคสยิง API โดยตรงเพื่อทดสอบการตีกลับ (Error 400) หากส่งข้อมูลยาวเกินไป, ส่งค่า Priority ผิด, หรือข้อมูลไม่ครบ | https://github.com/Kamonnatt23/toktickit/pull/23 |
| PR #24 feat: implement My tickets and unified navigation bar | Piink7878 | requested change | มีจุดที่อยากให้เช็กเพิ่มคือ<br><br>ใน App.tsx ตอนนี้ `<CreateTicket />` ถูก render ทั้งก่อนและใน `{activeTab === 'create' ? ...}` ทำให้หน้า Create Ticket น่าจะขึ้น form ซ้ำ 2 รอบ ตรงนี้น่าจะลบตัวที่ซ้ำออก<br>Test ที่ชื่อ filters by status and sorts ตอนนี้เช็กแค่ status filter ยังไม่ได้ verify การ sort จริง ๆ แนะนำเพิ่ม test สำหรับ sort asc/desc ให้ครบตาม feature ที่ทำ<br>Pagination มี implementation แล้ว แต่ยังไม่เห็น test สำหรับ Next/Previous แนะนำเพิ่ม test ตรงนี้ด้วย จะได้ verify ว่าเปลี่ยน page และเรียก API ถูก<br>ถ้ามีเวลาแนะนำเพิ่ม API error case ของ My Tickets ด้วยตอนนี้มี UI error state แล้วแต่ test ยังไม่ได้ cover<br>ส่วน ownership, search, pagination ฝั่ง API และ loading/empty state ดูโอเค<br><br>อีกอย่างรบกวนฝากแก้ให้เติม Navbar ที่มีโลโก้ TokTickIT, เมนูนำทาง และจุดที่แสดงชื่อ Requester ปัจจุบันพร้อมปุ่มสลับ User ตาม Requirement ด้วยนะ | แก้ไขเรียบร้อย เข้ามาตรวจหน่อยค้าบ | https://github.com/Kamonnatt23/toktickit/pull/24 |
| PR #25 feat: implement read-only ticket detail view and ownership validation | Piink7878 | requested change | มี 2 จุด<br><br>ใน App.tsx ตอนนี้ `<MyTickets />` ถูก render ทั้งใน ternary เดิมและใน `{activeTab === 'list' && ...}` ทำให้ตอนอยู่ My Tickets มีการ render ซ้ำ และตัวแรกไม่ได้ส่ง onTicketClick ไปด้วย แนะนำให้เหลือ conditional render ชุดเดียว<br>ฝั่ง API มี ownership check 403 แล้ว แต่ใน ticket-detail.api.test.ts ยังไม่เห็น test ที่ requester คนหนึ่งพยายามเปิด ticket ของ requester อีกคน แนะนำเพิ่มเคสนี้เพื่อ verify ownership requirement โดยตรง | แก้ไขเรียบร้อยค่ะ | https://github.com/Kamonnatt23/toktickit/pull/25 |
| PR #26 feat: implement attachment management (Issue 6) | Piink7878 | requested change | มี 2 จุดที่อยากให้แก้ก่อน approve<br><br>ตอนนี้ backend validate file type จาก mimetype อย่างเดียว แต่ requirement ระบุ allowed extensions เป็น JPG/JPEG/PNG/WEBP/PDF ด้วย แนะนำให้ validate extension เพิ่มด้วย<br><br>ตอน soft delete ตอนนี้ TicketDetail เพิ่ม UI สำหรับแสดง attachment ที่ isDeleted แล้ว แต่ /api/tickets/:id ยัง query เฉพาะ isDeleted: false ทำให้ deleted attachment ไม่ถูกส่งมาถึง UI เลย เลยไม่สามารถแสดง Removed: ... ที่เพิ่มมาได้ ควรปรับให้ API กับ UI behavior ตรงกัน<br><br>ส่วน test อยากให้เพิ่มกรณี 5 MB, ครบ 5 active attachments, empty removal reason และ ownership ตอน upload ด้วยจะได้ครอบคลุม requirement ของ Issue 6 มากขึ้น | แก้แล้ว | https://github.com/Kamonnatt23/toktickit/pull/26 |
| PR #27 fix(issue-7): Fix e2e tests, UI bugs and complete E2E testing | Piink7878 | requested change | มี 2 จุดที่อยากให้แก้<br><br>responsive.spec.ts ตอนนี้เปลี่ยน viewport เป็น Desktop/Tablet/Mobile แล้ว screenshot แต่ยังไม่มี assertion ที่ตรวจว่า UI responsive จริง เช่น ไม่มี horizontal overflow หรือ element สำคัญยัง visible อยู่ ทำให้ test ตอนนี้พิสูจน์ได้แค่ว่า screenshot ได้ครับ แนะนำเพิ่ม responsive assertion เข้าไป<br><br>ใน full-flow.spec.ts ตรง Download ตอนนี้เช็กแค่ว่า Download button มีอยู่ แต่ยังไม่ได้ verify ว่าการ download ทำงานจริง แนะนำใช้ Playwright waitForEvent('download') แล้วกดปุ่มเพื่อเช็กว่า download สำเร็จ<br><br>เพิ่มเติม ถ้า client/dummy.js ไม่ได้ใช้จริง แนะนำลบออกจาก PR ด้วยเพราะดูเหมือนเป็นไฟล์ทดลองมากกว่าไฟล์ที่จำเป็นกับ project | แก้แระ | https://github.com/Kamonnatt23/toktickit/pull/27 |

## Pull Requests I reviewed for my partner
| Partner PR | Decision | My Comment | Partner Response | Evidence |
|------------|----------|------------|------------------|----------|
| PR #24 Feature: Sprint Engineering Specification & Test Plan (Issue 1) | requested changes | ขอให้ clarify X-Requester-Id กับ requesterId นิดนึงนะ<br>ตอนนี้ใน API spec เหมือนจะบอกว่า requester สามารถมาจาก X-Requester-Id หรือ requesterId ใน body ก็ได้ แต่ด้าน validation ก็ระบุว่าทั้งสองค่าต้อง match กัน เลยอาจทำให้ตอน implement งงนิดนึงว่าอันไหนเป็นตัวหลัก 😅<br>แนะนำกำหนดให้ชัดไปเลยว่า X-Requester-Id เป็น source of truth แล้ว requesterId ใน body ต้องตรงกับ header (ถ้าจะเก็บไว้) หรือไม่ก็เลือกใช้แค่ทางใดทางหนึ่ง จะได้ไม่เกิดความเข้าใจไม่ตรงกันตอนทำ API ค่ะ<br><br>Query parameter ของ My Tickets ยังไม่ตรงกันระหว่าง specification.md กับ api-spec.md<br>ใน api-spec.md ใช้ requestedPriority, itPriority, pageSize แต่ใน specification.md ตรง API Contract Summary ยังใช้ priority, limit อยู่<br>อันนี้แนะนำให้เลือกชื่อให้เหลือชุดเดียว แล้วแก้ให้ตรงกันทั้งสองไฟล์เลย เพราะถ้าปล่อยไว้ตอน frontend/backend implement อาจใช้ parameter คนละชื่อกันได้ 😅 | commit "<br>docs: address follow-up PR feedback for API specs" | https://github.com/Piink7878/cpe334-TokTikIT/pull/24 |
| PR #25 Feature: Database Schema Increment & Seed Data (Issue 2) | requested changes | ขอแก้ seed.ts ตรง flow ของ main() นิดนึง<br><br>ตอนนี้เหมือน prisma.$disconnect() ใน finally ถูกเรียกก่อนที่จะ seed RelatedSystem กับ DevelopmentRequester เสร็จ แล้วด้านล่างก็มีการเรียก main() ซ้ำ 2 รอบด้วย (main(); และ main().catch(...))<br><br>ตรงนี้อาจทำให้ seed พยายามใช้ Prisma หลังจาก disconnect ไปแล้ว และอาจทำให้การรัน seed ไม่ได้เป็นไปตามที่ตั้งใจ<br><br>แนะนำให้เหลือ main() แค่ครั้งเดียว แล้วให้ try/catch/finally ครอบ seed ทั้ง Categories, Related Systems และ Development Requesters ทั้งหมดก่อนค่อย $disconnect() ทีเดียวค่ะ<br><br>นอกนั้น schema กับ migration โดยรวมดูโอเคเลย 👍 | แก้ไขโค้ดให้รวบไว้ใน try/catch เดียว และลบการเรียก main() ที่ซ้ำซ้อนออกเรียบร้อยแล้ว รบกวนรีวิวอีกรอบค่าา | https://github.com/Piink7878/cpe334-TokTikIT/pull/25 |
| PR #26 Feature: Development Requester Selector & Context (Issue 3) | requested changes | ขอเพิ่ม persistence ให้ RequesterContext อีกนิดนึงง 🙏<br><br>ตอนนี้ selectedRequester เก็บด้วย useState อย่างเดียว ซึ่งทำให้ context อยู่ได้ตอน navigate ระหว่างหน้า แต่ถ้า user กด refresh (F5) หน้าเว็บ selectedRequester จะกลับเป็น null แล้ว ProtectedRoute ก็จะ redirect กลับไปหน้าเลือก requester ใหม่ 😅<br><br>เนื่องจาก requirement ของ Issue 3 เป็นการเก็บ Requester Context แนะนำให้ persist ค่า requester ไว้ใน localStorage หรือ sessionStorage แล้วตอน RequesterProvider mount ให้โหลดค่ากลับมา จะได้รักษา requester context หลัง refresh ด้วย<br><br>ส่วน API /api/requesters กับหน้า Selection และ Change Requester โดยรวมดูโอเคแล้ว 👍 | เพิ่ม persistence ด้วย localStorage เข้าไปใน RequesterProvider แล้ว ตอนนี้กด F5 หรือ refresh หน้าเว็บแล้ว Context ไม่หลุดแล้ว และตอนกด Change Requester ก็เคลียร์ storage ออกให้ปกติเลย รบกวนรีวิวอีกรอบนะ | https://github.com/Piink7878/cpe334-TokTikIT/pull/26 |
| PR #27 Feature: Ticket Creation Feature API & UI (Issue 4) | requested changes | ขอเช็กเรื่อง Ticket Number uniqueness เพิ่มนิดนึง<br>ตอนนี้ logic จะ findFirst() หา ticket ล่าสุด → เอา sequence +1 → แล้วค่อย create ticket ซึ่งถ้ามี request เข้ามาสร้างพร้อมกัน 2 ตัว อาจมีโอกาสที่ทั้งสอง request อ่านเลขล่าสุดตัวเดียวกัน แล้ว generate Ticket Number เดียวกันได้<br><br>เพราะ requirement ระบุว่า Ticket Number ต้อง unique อัตโนมัติ แนะนำลองพิจารณาวิธีที่รองรับ concurrent request ด้วย เช่นใช้ DB sequence/transaction หรือมี unique constraint + retry กรณีเลขชนกัน<br><br>Test ของ Create Ticket ตอนนี้มี flow หลักแล้ว แต่ขอเพิ่ม edge cases อีกนิด<br>ตอนนี้มี success, missing requester header, summary สั้น และ category ไม่พบแล้ว 👍 แต่ถ้าจะให้ครอบคลุมตาม feature มากขึ้น อยากให้เพิ่ม test พวก relatedSystemId ไม่พบ, invalid priority, requester ID mismatch และ boundary ของ summary/description ด้วย<br><br>แล้วส่วน attachment ตอนนี้มีการจำกัด 5 ไฟล์, 5MB และ allowed MIME type ใน upload middleware แล้ว แต่ test ยัง mock upload ให้ผ่านตลอด เลยยังไม่ได้ verify พวก file type / file size / จำนวนไฟล์จริง ๆ ถ้าเพิ่มได้จะช่วยให้ test coverage ของ Issue 4 แน่นขึ้นมาก | แก้ไขระบบรันเลขให้รองรับ Concurrency ด้วย Retry Logic + Unique Constraint แล้ว พร้อมทั้งเพิ่ม Test Cases คลุม Edge Cases และยิงไฟล์จริงผ่าน Supertest เรียบร้อยแล้ว รบกวนรีวิวอีกรอบนะ | https://github.com/Piink7878/cpe334-TokTikIT/pull/27 |
| PR #28 Feature: My Tickets List & Search (Issue 5) | requested changes | โดยรวม implementation ของ My Tickets ทำมาครบดี ทั้ง ownership, search, filter, sort, pagination รวมถึง loading / empty / no-results state<br><br>มีเรื่อง Test Coverage อยากให้เพิ่มอีกนิด ตอนนี้ my-tickets.api.test.ts มี test หลัก ๆ แล้ว แต่ยังไม่ได้แยก test เพื่อยืนยัน behavior ของ Search / Filter / Sort / Pagination แต่ละส่วนอย่างชัดเจน เช่น<br><br>Search แล้วได้เฉพาะ ticket ที่ match<br>Filter ตาม status/category/priority<br>Sort ทั้ง asc และ desc<br>Pagination หลายหน้า รวมถึง hasNextPage / hasPreviousPage<br>Frontend interaction ว่าเมื่อเปลี่ยน Search / Filter / Sort / Pagination แล้ว getTickets() ถูกเรียกด้วย parameters ที่ถูกต้อง<br>ตอนนี้มี combined query test ที่ส่ง parameters หลายตัวพร้อมกัน ซึ่งช่วยตรวจว่า query ถูกประกอบถูกต้อง แต่ยังไม่ค่อยครอบคลุม behavior ของแต่ละ feature<br><br>รบกวนเพิ่ม test coverage ส่วนนี้อีกนิด เพื่อให้ทุก Acceptance Criteria ของ Issue มี test รองรับครบ | เราแก้และ รบกวนเข้ามาตรวจอีกรอบบ | https://github.com/Piink7878/cpe334-TokTikIT/pull/28 |
| PR #29 | requested changes | ขอเช็ก requester validation ใน Attachment API เพิ่มนิดนึงนะ<br>ตอนนี้ POST /api/tickets/:id/attachments, GET /api/attachments/:id/download และ DELETE /api/attachments/:id เช็กว่า X-Requester-Id มีค่าและเป็นตัวเลขแล้ว แต่ยังไม่ได้เช็กว่า requester มีอยู่จริง / active ใน DB ก่อนทำ ownership check<br><br>เช่นส่ง X-Requester-Id: 99999 ที่ไม่มี requester จริง ตอนนี้จะไหลไปถึง ownership check แล้วอาจกลายเป็น 403 ซึ่ง behavior ไม่ค่อย consistent กับ API contract ของโปรเจกต์<br><br>แนะนำให้เพิ่ม requester existence/active check ให้ Attachment endpoints ด้วย จะได้ behavior เหมือน endpoint อื่น ๆ<br><br>Attachment tests ครบหลายเคสแล้ว 👍 แต่อยากเพิ่ม boundary/failure cases อีกนิด<br>ตอนนี้มี upload success, invalid type, >5MB, 5-file limit, ownership, download และ soft removal แล้ว ถือว่าครบขึ้นเยอะมาก<br><br>ที่อยากเพิ่มคือ exactly 5MB ต้อง upload ได้ เพื่อเช็ก boundary ของ limit และเพิ่ม ticket not found / attachment not found cases อีกนิด จะได้ครอบคลุม failure cases ของ lifecycle มากขึ้นค่ะ | แก้แล้ววว รบกวนตรวจอีกรอบค่า | https://github.com/Piink7878/cpe334-TokTikIT/pull/29 |
| PR #30 Feature: Responsive UI Verification & E2E Testing (Issue 7) | requested changes | เยี่ยมไปเลย แต่ขอคอมเม้นนิดนึงนะ<br>1.E2E flow ตอนนี้ไปถึง Ticket Detail แล้ว แต่ยังไม่ได้ cover Attachment flow<br><br>ตอนนี้ test ครบถึง Select Requester → Create Ticket → Search My Tickets → Open Ticket Detail แล้ว แต่ใน Lab 2 product flow ยังมี Attachment lifecycle ด้วย<br><br>ไม่ต้อง implement attachment ซ้ำใน Issue 7 นะ แนะนำแค่เพิ่ม E2E step ที่ Ticket Detail เพื่อ verify attachment ที่ทำไว้ใน Issue 6 เช่น upload → แสดง metadata → download/remove ตาม scope ที่ทำไว้ จะได้เป็น end-to-end flow ที่ครอบคลุมมากขึ้น<br><br>Responsive screenshot มีครบแล้ว แต่ขอเพิ่ม visual verification อีกนิด<br>ตอนนี้มี screenshot ครบ Desktop / Tablet / Mobile แล้ว ถือว่าดีมาก แต่ Lab 2 ยังต้องมี visual checklist ตรวจพวก horizontal overflow, clipping, overlap, hidden buttons และ attachment names ด้วย<br><br>แนะนำเพิ่ม checklist หรือ automated assertion เล็ก ๆ เช่นเช็ก scrollWidth <= clientWidth และองค์ประกอบสำคัญไม่ overflow ในแต่ละ viewport จะได้พิสูจน์ว่า responsive ไม่ใช่แค่มี screenshot แต่ layout ใช้งานได้จริง | ทำเพิ่มเร่บร้อย ช่วยเช็คด้วยยย | https://github.com/Piink7878/cpe334-TokTikIT/pull/30 |